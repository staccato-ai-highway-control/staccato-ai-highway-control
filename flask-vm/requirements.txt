# Flask 웹 API, 실시간 통신, ORM 및 마이그레이션 런타임 의존성입니다.
# 호환 가능한 마이너 버전 업데이트는 허용하되 다음 메이저 버전은 차단합니다.
Flask>=3.0,<4.0
Flask-Cors>=4.0,<5.0
Flask-SocketIO>=5.3,<6.0
SQLAlchemy>=2.0,<3.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Migrate>=4.0,<5.0
PyMySQL>=1.1,<2.0
python-dotenv>=1.0,<2.0
pydantic>=2.7,<3.0
PyJWT>=2.8,<3.0
cryptography>=42.0,<46.0
requests>=2.31,<3.0

# 테스트 실행 의존성입니다.
pytest>=8.0,<9.0

# 운영 WSGI 프로세스는 배포 재현성을 위해 정확한 버전을 고정합니다.
gunicorn==26.0.0
