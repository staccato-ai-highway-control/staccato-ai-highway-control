FROM python:3.11-slim

WORKDIR /app

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

EXPOSE 8001

CMD ["sh", "-c", "echo 'AI server placeholder. Add FastAPI, YOLO, OpenCV later.' && sleep infinity"]